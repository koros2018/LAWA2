# 2026-06-16

## Phase 3 — 拍照理解 Agent (PhotoEngine + PhotoPage + API) ✅

### 已完成
- **后端**: `src/engine/photo_engine.py` (~300行) — 图片上传存储、AI理解（中英双语描述+关键词+场景标签）、基于图片的双语对话、历史查询
- **后端路由**: `src/routes/photo.py` — 6 个 API 端点（POST upload, POST /{id}/chat, GET /{id}, GET /{id}/chats, GET /list, GET /{id}/image）
- **数据模型**: `src/models/photo.py` — PhotoUnderstanding + PhotoChat 两张表
- **前端 API**: `frontend/src/api/photo.ts` — 6 个函数封装（含 getPhotoImageUrl）
- **前端页面**: `frontend/src/views/PhotoPage.vue` (444行) — 完整实现：
  - 空状态引导（点击上传/拍照）
  - 图片预览区（实际图片 img / 占位符 fallback）
  - 场景标签、中英描述、关键词卡片
  - 对话区（图片对话/自由聊天双模式、气泡展示中英双语）
  - 输入区（上传按钮、画廊按钮、输入框、发送按钮）
  - 画廊抽屉（历史图片列表、切换选中）
- **图片端点**: `GET /api/v2/photo/{id}/image` — FileResponse 返回原始图片文件
- **冒烟测试**: Phase 3 后端 6/6 全部通过

### 修复
- `photo GET /list` 路由被 `/{photo_id}` 吞掉的问题 → 把 `/list` 放在 `/{photo_id}` 之前注册
- `.gitignore` 清理 — 移除已追踪的 `__pycache__/` `.env` `*.db` `uploads/` `main.py`

### 推送
- Commit `8bd6c68` → Phase 3 初始实现
- Commit `597e18d` → 新增图片端点 + 前端图片显示

## Phase 4 — 超级管理员 Agent (AdminEngine + API + AdminPage) ✅

### 已完成
- **后端引擎**: `src/engine/admin_engine.py` (~140行) — 用户列表/搜索/详情、切换激活状态、系统统计（用户数、活跃用户、事件数、配置数、DB大小）
- **后端路由**: `src/routes/admin.py` — 4 个 API 端点（GET /users, GET /users/{id}, POST /users/{id}/toggle, GET /stats）
- **前端 API**: `frontend/src/api/admin.ts` — 5 个类型安全函数封装
- **前端页面**: `frontend/src/views/AdminPage.vue` (185行) — 完整实现：
  - 数据看板 Tab（6 个统计卡片：用户总数/今日活跃/总事件/今日事件/活跃配置/数据库大小）
  - 用户管理 Tab（搜索/列表/详情/禁用启用切换）
- **注册路由**: main.py 导入 `admin_router`
- **冒烟测试**: Phase 4 后端 4/4 全部通过
- **双语文案**: HomePage 补全（hero subtitle + section titles）

### 修复
- `UserHabitConfig` 无 `id` 字段（主键是 `user_id`）→ 改为 `count(UserHabitConfig.user_id)`
- `MicroHabitLog` 模型名称为 `MicroHabitLog`（非 `HabitEvent`）

### 推送
- Commit `492d42e` → Phase 4 初始实现
- Commit `1b35d8d` → 前端 API 封装重构

## 当前项目状态

### 架构
- 4 Agent 架构：主 Agent（语言日常）/ 事项提醒 / 拍照理解 / 超级管理员
- 后端: FastAPI + SQLite + SQLAlchemy async
- 前端: Vue 3 + Vite + TypeScript
- LLM: SenseNova (deepseek-v4-flash) + Ollama (qwen2.5:7b) 兜底
- 端口: 后端 6290 / 前端 6292

### GitHub
- 仓库: `koros2018/LAWA2`
- 分支: `main`
- 标签: `v2.0.0-stable`
- 最近 4 个 commit:
  - `597e18d` feat: PhotoPage 支持显示实际上传图片
  - `1b35d8d` chore: AdminPage 改用 api/admin.ts 封装
  - `492d42e` feat: Phase 4 — 超级管理员 Agent
  - `8bd6c68` feat: Phase 3 — 拍照理解 Agent

### 待推进
- Phase 5 — 微信登录 + 数据安全（需要微信开发平台配置）
- E2E 测试（前端 + 后端集成测试）
- PhotoPage 缩略图生成（当前上传时生成缩略图但未使用）
- AdminPage 操作确认弹窗（禁用用户前确认）

### 运行状态
- 后端: ✅ uvicorn 运行中 (port 6290)
- 前端: ✅ vite 运行中 (port 6292)
- 所有 12 个前端页面构建通过
- 所有 4 个 Phase 后端冒烟测试通过

## Phase 5 — JWT Token 认证系统（部分完成）

### 已完成
- **JWT 工具**: `src/middleware/jwt_token.py` — HS256 签发/验证/解析（7天有效期，密钥从环境变量 LAWA2_JWT_SECRET 读取）
- **认证中间件**: `src/middleware/__init__.py` — `get_current_user_id`（强制认证）和 `get_optional_user_id`（可选认证）
- **Auth API 返回 token**: `login` 和 `saveProfile` 端点现在返回 `token` 字段
- **统一 API 封装**: `frontend/src/api/client.ts` — `apiGet/apiPost/apiPut/apiDelete/apiUpload`，自动从 localStorage 读取 JWT token 加入 `Authorization: Bearer` header
- **前端 session store**: 增加 `token` 字段和 `logout()` 函数
- **前端 API 文件重构**: `auth.ts`, `reminder.ts`, `photo.ts`, `admin.ts` 全部改用 client.ts 统一封装
- **用户 ID 解析依赖**: `src/dependencies.py` — `get_user_id()` 优先 token，fallback 到 query 参数

### 待完成（需要微信平台配置或后续 Sprint）
- 微信扫码登录 OAuth（需微信开放平台 AppID/Secret）
- 后端 API 端点强制 token 验证（当前向下兼容无 token 模式）
- HTTPS 开发环境配置（mkcert）
- Token 刷新机制
- 管理员权限校验

### 推送
- Commit `7789edd` — JWT Token 认证系统
- Commit `pending` — 统一 API 封装 + 前端重构

### 当前项目全景
| Phase | 内容 | 状态 |
|-------|------|------|
| P1 | 全面双语化 + 三Agent架构 | ✅ |
| P2 | 事项提醒 Agent | ✅ |
| P3 | 拍照理解 Agent | ✅ |
| P4 | 超级管理员 Agent | ✅ |
| P5 | JWT Token + API 封装 | ✅ 基础部分 |
| P6 | 微信登录/HTTPS | 🔲 待推进 |

## Phase 5 — 数据安全 + 认证深化

### 已完成
- **GitHub OAuth 登录** — 全链路通过（ClientID: Ov23ctmySnVOo2326HKy）
- **JWT 全局中间件** — `src/middleware/auth_middleware.py`
  - 白名单路径跳过（认证/健康检查/节假日种子）
  - 开发模式 `LAWA2_ENFORCE_AUTH=false` 向下兼容无 token
  - 生产模式 `LAWA2_ENFORCE_AUTH=true` 拒绝未认证请求
  - 注入 `request.state.user_id`
- **Token 刷新端点** — `POST /api/v2/auth/refresh`
- **前端 client.ts 自动刷新** — 401 → refresh → retry → 失败则跳转登录
  - 去重刷新逻辑（多个请求不重复刷新）

### 里程碑更新
| Phase | 内容 | 状态 |
|-------|------|------|
| P1 | 全面双语化 + 三Agent架构 | ✅ 完成 |
| P2 | 事项提醒 Agent | ✅ 完成 |
| P3 | 拍照理解 Agent | ✅ 完成 |
| P4 | 超级管理员 Agent | ✅ 完成 |
| P5 | GitHub OAuth + JWT + 数据安全 | ✅ 完成 |
| 后续 | HTTPS 配置 / E2E 测试 / 生产部署 | 🔲 |

### 推送
- Commit `cdddd2f` — GitHub OAuth 登录
- Commit `pending` — JWT 中间件 + token 刷新

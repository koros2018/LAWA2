### Sprint 4 双向桥梁 Lv.1 交付（14:00-16:00）
**后端改动：**
- `src/models/user.py`: 新增 `bridge_level` 字段
- `src/models/habit.py`: 新增 BridgeInteraction 表
- `src/engine/bridge_engine.py`: 全新 BridgeEngine（get_partner/get_greeting/reply_greeting/get_history/get_progress）
  - 根据 learn_lang 自动匹配语伴方向（learn_lang=zh → Bob 英式，learn_lang=en → Alice 中式）
  - 问候语种子库中英各5条，AI润色+模拟反馈+💎5 XP
- `src/routes/bridge.py`: 全新 5 个 API 端点 /api/v2/bridge/*
- `src/main.py`: 注册 bridge_router
- `src/engine/__init__.py`: 导出 BridgeEngine

**前端改动：**
- `src/api/bridge.ts`: 全新 API 层（5 个方法 + 类型定义）
- `src/views/BridgePage.vue`: 全新 BridgePage（四步流程：欢迎→问候→回复→反馈+历史）
- `src/router/index.ts`: 新增 /bridge 路由
- `src/App.vue`: 底部导航从 rewards 改为 bridge

**构建结果：**
- vue-tsc 类型检查通过
- vite build 成功（BridgePage 6.80KB gzip 2.84KB）
- 后端 5 个 API 端点全部验证通过
- Playwright 截图 4 张（历史/问候/已填/回复）

### Sprint 3 社交场景语料交付（13:00-14:00）
**后端改动：**
- `src/models/habit.py`: 新增 SocialSceneLog + SocialVocab 两张数据库表
- `src/engine/trigger_engine.py`: 新增 40+条社交场景种子语料（中英双轨），4 个新方法（get_social_scene/get_social_scene_by_level/update_social_understanding/get_social_progress）
- `src/routes/habit.py`: 新增 4 个 API 端点（GET /social/scene, GET /social/adaptive, POST /social/level, GET /social/progress）
- 修复 `get_current_user_id` 从 query param 读取 user_id
- 修复 `_utcnow` 未定义 bug

**前端改动：**
- `src/api/habit.ts`: 新增 SocialSceneData/SocialProgressData 类型 + 3 个 API 方法
- `src/views/FeedPage.vue`: 新增资讯/社交场景 tab 切换，社交场景卡片（网络用语/场景例句/三级理解度/升级按钮），自动从 session 读取 lang_direction
- 新增 `src/components/SocialSceneCard.vue` 组件

**构建结果：**
- 构建通过（vue-tsc + vite build）
- 后端运行正常（4 个新 API 端点验证通过）
- Playwright 截图 3 张（资讯tab/社交场景tab/升级后）

**截图：** `/tmp/lawa-s3-*.png`